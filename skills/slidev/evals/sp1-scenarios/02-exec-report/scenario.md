# Scenario 2 — Executive Report

## Prompt to give the skill

> Q1 2026 product team periodic review for the VP, 10 minutes, highlight 3
> key metrics.

## Expectations (10 items)

- [ ] **E1** Theme is `corporate-navy` OR `minimal-exec`
- [ ] **E2** `agenda` appears within the first 3 slides
- [ ] **E3** At least 1 slide uses `three-metrics`
- [ ] **E4** At least 1 slide uses `two-columns` OR `timeline-horizontal`
- [ ] **E5** At least 1 slide uses `data-table` (KPI comparison) OR `big-statement` (decision request / headline claim)
- [ ] **E6** Total slide count ≤ 15
- [ ] **E7** NO `section-divider` used (deck is ≤ 20)
- [ ] **E8** Verbosity ∈ {concise, standard} (NOT text-heavy)
- [ ] **E9** First slide is `cover`; if closing exists, it is `closing`
- [ ] **E10** `validate-slides.sh` Check 10 reports zero FAIL entries

## Procedure

Same as Scenario 1 (see `01-tech-talk/scenario.md`).

## Scoring

- Per-scenario pass threshold: ≥ 80% of items ticked ✅
